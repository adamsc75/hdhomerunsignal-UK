version: '3.8'

services:
  hdhomerun-signal:
    build: .
#    ports:
#      - "3000:3000"
    network_mode: host
    restart: unless-stopped
    environment:
      - NODE_ENV=production
    volumes:
      - /etc/localtime:/etc/localtime:ro
